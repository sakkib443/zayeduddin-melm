export const metadata = {
    title: "About",
    description: "Learn about ejobs it - a leading IT training institute and digital solutions provider in Bangladesh.",
};

export default function AboutLayout({ children }) {
    return children;
}
